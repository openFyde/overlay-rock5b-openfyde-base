description     "Setup power manager policy to block suspend"
author          "yang@fydeos.io"

start on started system-services or started smbproviderd
oom score -100

script
  logger -t "${UPSTART_JOB}" "setup power manager suspend to s2idle"
  echo s2idle > /var/lib/power_manager/suspend_mode
end script
